# Django Logging Example

This is a minimal Django project demonstrating how to configure and use logging at all standard levels: `DEBUG`, `INFO`, `WARNING`, `ERROR`, and `CRITICAL`.

## Features

- Logs messages to both the console and a file (`project.log`)
- Uses different formatters for console and file outputs
- Demonstrates logging inside a Django view